<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans leading-normal tracking-normal">
    <div class="container w-full md:max-w-3xl mx-auto pt-10 px-6">
        <div class="w-full">
            <h1 class="text-3xl font-bold text-gray-800 mb-4">{{ title }}</h1>

            {% if description %}
            <div class="text-gray-600 italic mb-6">{{ description }}</div>
            {% endif %}

            <ul class="space-y-3">
                {% for name, url in links %}
                <li class="hover:bg-gray-50 rounded p-2 transition-colors duration-200">
                    <a href="{{ url }}" target="_blank" class="text-blue-600 hover:text-blue-800 hover:underline">{{ name }}</a>
                </li>
                {% endfor %}
            </ul>

            <div class="text-center text-gray-500 text-sm mt-10 pt-4 border-t border-gray-200">
                Generated by MiniBook on {{ timestamp }}
            </div>
        </div>
    </div>
</body>
</html>
